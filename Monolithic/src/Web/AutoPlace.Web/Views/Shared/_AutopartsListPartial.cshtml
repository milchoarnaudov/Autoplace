@model IEnumerable<AutopartsListItemViewModel>

<div class="autoparts-list-container">
    @if (Model.Count() == 0)
    {
        <div>Empty</div>
    }
    else
    {
        <div class="autoparts-list">
            @foreach (var autopart in Model)
            {
                <partial name="_AutopartPartial" model="autopart" />
            }
        </div>
        <br />
    }
</div>