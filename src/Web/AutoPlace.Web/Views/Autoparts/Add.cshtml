@model CreateAutopartInputModel
@{
    this.ViewData["Title"] = "Add Autopart";
    var categoryItems = this.Model.Categories.Select(x => new SelectListItem(x.Value, x.Key.ToString()));
    var conditionItems = this.Model.Conditions.Select(x => new SelectListItem(x.Value, x.Key.ToString()));
    var carManufacturerItems = this.Model.CarManufacturers.Select(x => new SelectListItem(x.Value, x.Key.ToString()));
    var carTypes = this.Model.CarTypes.Select(x => new SelectListItem(x.Value, x.Key.ToString()));
}
<h1 class="text-center header">Add Autopart</h1>
<br />

<form method="post" class="col-md-6 offset-md-3" enctype="multipart/form-data">
    <div class="form-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Price"></label>
        <span class="input-group-addon">&euro;</span>
        <input asp-for="Price" asp-format="{0}" value="" class="form-control" />
        <span asp-validation-for="Price" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Description"></label>
        <textarea asp-for="Description" class="form-control" rows="6"></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="ConditionId"></label>
        <select asp-for="ConditionId" class="form-control" asp-items="conditionItems"></select>
        <span asp-validation-for="ConditionId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="CategoryId"></label>
        <select asp-for="CategoryId" class="form-control" asp-items="categoryItems"></select>
        <span asp-validation-for="CategoryId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="CarManufacturerId"></label>
        <select asp-for="CarManufacturerId" class="form-control" asp-items="carManufacturerItems"></select>
        <span asp-validation-for="CarManufacturerId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="ModelId"></label>
        <select asp-for="ModelId" class="form-control"></select>
        <span asp-validation-for="ModelId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="CarTypeId"></label>
        <select asp-for="CarTypeId" class="form-control" asp-items="carTypes"></select>
        <span asp-validation-for="CarTypeId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="CarMakeYear"></label>
        <input asp-for="CarMakeYear" value="" class="form-control" />
        <span asp-validation-for="CarMakeYear" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Images"></label>
        <input asp-for="Images" class="form-control-plaintext" />
        <span asp-validation-for="Images" class="text-danger"></span>
    </div>
    <input type="submit" class="btn btn-primary" />
</form>

@section Scripts {
    <script>
        let carManufacturer = document.getElementById('CarManufacturerId');
        let carModel = document.getElementById('ModelId');

        fetchData(carManufacturer.value);

        carManufacturer.addEventListener('change', (e) => {
            fetchData(e.target.value);
        });

        function fetchData(target) {
            carModel.innerHTML = "";
            fetch(`/autoparts/getModelsById?Id=${target}`)
                .then((response) => {
                    return response.json();
                })
                .then((data) => {
                    Array.prototype.forEach.call(data, function (item, i) {
                        carModel.innerHTML += `<option value="${item.key}">${item.value}</option>`
                    });
                });
        }
    </script>
}